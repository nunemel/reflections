When would you want to use a remote repository rather than keeping all your work
local?

    When I want to collaborate with other people and share my files.

Why might you want to always pull changes manually rather than having Git
automatically stay up-to-date with your remote repository?

    When I don't want to send my remote changes to my local machine.

Describe the differences between forks, clones, and branches.  When would you
use one instead of another?

    Forking means copiing repos from another github places, it operates on github. 
    Cloning means copiing repos remotely or locally.
    Branches happen in single repo, you can push and pull branches, but when you create one, you're creating it on one single repo. 
    Git allows create lables for commits. These labels are called branches.

What is the benefit of having a copy of the last known state of the remote
stored locally?

    After being offline awile you can pull changes from remote and be up-to-date with remote repo.

How would you collaborate without using Git or GitHub?  What would be easier,
and what would be harder?

    Using email or social sites. Code review, sharing would be very hard, but messageing with each other would be easy.

When would you want to make changes in a separate branch rather than directly in
master?  What benefits does each approach have?

    Fill in your answer here
